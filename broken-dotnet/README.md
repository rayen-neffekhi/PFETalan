This repository contains a deliberately broken .NET sample used to test code-review workflows.

Contents:
- `BrokenApp` - Console app with compile-time and logic errors.
- `Broken.Tests` - xUnit tests (one test intentionally fails).

Do not fix these errors — they are intentional for testing.
